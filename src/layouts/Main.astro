---
import Breadcrumb from "@/components/Breadcrumb.astro";

type StringTitle = { pageTitle: string };
type ArrayTitle = { pageTitle: [string, string]; titleTransition: string };

type Props = (StringTitle | ArrayTitle) & { pageDesc?: string };

const { props } = Astro;
---

<Breadcrumb />
<main id="main-content" class="app-layout pb-8">
  {
    "titleTransition" in props ? (
      <h1 class="text-2xl font-semibold tracking-tight">
        {props.pageTitle[0]}
        <span transition:name={props.titleTransition}>
          {props.pageTitle[1]}
        </span>
      </h1>
    ) : (
      <h1 class="text-2xl font-semibold tracking-tight">{props.pageTitle}</h1>
    )
  }
  <p class="mt-2 mb-6 font-mono text-[0.8125rem] text-foreground/60">
    {props.pageDesc}
  </p>
  <slot />
</main>
